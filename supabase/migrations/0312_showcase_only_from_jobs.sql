-- The booking page's before and afters come only from the app: the posts
-- approved and formatted in Before & After Posts from the crew's own job
-- photos. The separate list, which held the website's pictures, is gone.
drop table if exists booking_showcase;
