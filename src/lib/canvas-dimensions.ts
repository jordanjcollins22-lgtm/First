/** The fixed coordinate space zones are drawn/stored in — shared by the live
 * canvas board and anything that re-renders a saved site map (like a proposal). */
export const CANVAS_WIDTH = 1280;
export const CANVAS_HEIGHT = 800;
